<!--This Document is generated by GameMaker, if you edit it by hand then you do so at your own risk!-->
<object>
  <spriteName>&lt;undefined&gt;</spriteName>
  <solid>0</solid>
  <visible>-1</visible>
  <depth>0</depth>
  <persistent>0</persistent>
  <parentName>&lt;undefined&gt;</parentName>
  <maskName>&lt;undefined&gt;</maskName>
  <events>
    <event eventtype="0" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>background_xscale[0] = 1 * ((room_width) / background_width[0]);
background_yscale[0] = 1 * ((room_height) / background_height[0]);
background_xscale[1] = 1 * ((room_width) / background_width[1]);
background_yscale[1] = 1 * ((room_height) / background_height[1]);
background_xscale[2] = 1 * ((room_width) / background_width[2]);
background_yscale[2] = 1 * ((room_height) / background_height[2]);
background_xscale[3] = 1 * ((room_width) / background_width[3]);
background_yscale[3] = 1 * ((room_height) / background_height[3]);
background_xscale[4] = 1 * ((room_width) / background_width[4]);
background_yscale[4] = 1 * ((room_height) / background_height[4]);
background_xscale[5] = 1 * ((room_width) / background_width[5]);
background_yscale[5] = 1 * ((room_height) / background_height[5]);
tempRoom = room;
global.InputBoxX = global.roomWidth/5
global.InputBoxY = global.roomHeight/6
global.InputTextX = global.roomWidth/5
global.InputTextY = global.roomHeight/6
//This code is used for actually creating the text box instances to didplay on screen
global.tempInputBoxX = global.InputBoxX+149; //sets temp input box X
global.mapInputBoxX = global.roomWidth-100;

global.tempInputBoxY = global.InputBoxY;//sets temp input box Y
global.tempTextBoxY = global.InputTextY;//sets temp text box Y
global.textBoxInstanceRef = instance_count;//This is the number of objects before instances are created.

global.focus = 0; //box focus
if(room == RoomCustomStats)
{
   global.tempInputBoxY -= 52;
    global.tempTextBoxY -= 2;
    for(i = 0; i &lt; 4; i++)
    {    
        if(i != 0)
        {
            global.tempInputBoxY += 25;
            global.tempTextBoxY += 25;
        }
        if(i == 3)
        {
            global.tempInputBoxY += 25;
            global.tempTextBoxY += 25;
        }        
        global.textboxArray[i] = instance_create(global.roomWidth/2+149,global.tempInputBoxY+100,TextInputBox);         
    }
}
if(room == RoomMap)
{            
    global.tempInputBoxY -= 52;
    global.tempTextBoxY -= 2;
    for(i = 0; i &lt; 12; i++)
    {    
        if(i != 0)
        {
            global.tempInputBoxY += 25;
            global.tempTextBoxY += 25;
        }
        
        global.textboxArray[i] = instance_create(global.mapInputBoxX,global.tempInputBoxY,TextInputBox);         

    }
    
}
if(room == RoomTutorial)
{
        global.box0 = instance_count;
        global.box1 = instance_count+2;
        global.box2 = instance_count+4;
        global.box3 = instance_count+6;
        global.box4 = instance_count+8;
        global.box5 = instance_count+10;
        
        global.box6 = instance_count+12;
        global.box7 = instance_count+13;
        global.box8 = instance_count+14;
        global.box9 = instance_count+15;
        

        global.tempInputBoxY += 25;
        global.tempTextBoxY += 25;
    for(i = 0; i &lt; 10; i++)
    {            
            global.tempInputBoxY += 25;
            global.tempTextBoxY += 25;
        global.textboxArray[i] = instance_create(global.roomWidth/2-50,global.tempInputBoxY,TextInputBox);    
        if(i != 6 and i != 7 and i != 8 and i != 9)
            global.textboxArray3[i] = instance_create(global.roomWidth/2+170,global.tempInputBoxY,ConsType);           

    }
}
if(room == RoomLocationAdd)
{
    if(global.tempLocation != "LocationT")
    {
        global.tempInputBoxY += 85;
        global.tempTextBoxY += 85;
        //this displays the text on the location add screen (once you click on a location to edit its information)
        textFeilds = 4;//number of text boxes we want.
        ini_open(global.locationINI)//open ini file
        global.realLocationName = ini_read_string(global.tempLocation,"Real Location Name",""); //Reads the name of location from ini
        global.description = ini_read_string(global.tempLocation,"Description","");//reads the description from ini
        global.locationMyth = ini_read_real(global.tempLocation, "Mythical", "");//reads the mythical status from ini
        global.locationEventNumber = ini_read_real(global.tempLocation, "Number of Events",0); //reads the number of events from ini
        ini_close();//closes the ini
        for(i = 0; i &lt; textFeilds; i++)//for the number of text boxes set above
        {
            if(i == 1)//if i = description box then it will set the position based on the increased size
            { 
                global.textboxArray[i] = instance_create(global.tempInputBoxX,global.tempInputBoxY,TextInputBox);  //creats an instance at the location using the object 
                global.tempInputBoxY += 100;//increases the location of the text/input boxes (large increase for multiple line)
                global.tempTextBoxY += 100;
            }
            else
            {
                //creates the instanes of the text boxes on screen.
                global.textboxArray[i] = instance_create(global.tempInputBoxX ,global.tempInputBoxY,TextInputBox);  //creats an instance at the location using the object 
        
            }
                global.tempInputBoxY += 25; //increases the location of the text/input boxes (standard increase for one line)
                global.tempTextBoxY += 25;
        }
        if(global.locationEventNumber &gt; 0)//if there are events
        {
            global.tempInputBoxY += 75; //set the position 
            global.tempTextBoxY += 75;   
           
            tempEventID = string(global.EventID);
            global.tempEvent = "Event"+tempEventID;//setup the event name for accessig the ini
            tempEventType = global.tempEvent+"Type";
            tempEventWeek = global.tempEvent+"Week";
            tempEventYear = global.tempEvent+"Year";
            tempEventmyth = global.tempEvent+"Mythical";
            ini_open(global.locationINI);//open the ini
            global.eventType = ini_read_real(global.tempLocation, tempEventType, 0);//read the event type
            global.eventWeek = ini_read_real(global.tempLocation, tempEventWeek, 0);//read the event week
            global.eventYear = ini_read_real(global.tempLocation, tempEventYear, 1);//read the event week
            global.eventMyth = ini_read_real(global.tempLocation, tempEventmyth, 0);//read the event mythical status
            ini_close();
            global.oldWeek = global.eventWeek;
            global.locationWeekabc = global.eventWeek;
            for(i = 4; i &lt; 8; i++)//for the number of feilds (type,week,mythical)
            {
                global.textboxArray[i] = instance_create(global.tempInputBoxX ,global.tempInputBoxY,TextInputBox);  //create an instance at the location using the object 
                global.tempInputBoxY += 25; //incrase the position of the text boxes
                global.tempTextBoxY += 25;   
            }
            if(global.locationWeekabc != 0)
            {
                global.NumOfWeeksAtLocation = 0;
                global.tempInputBoxY -= 100; //incrase the position of the text boxes
                global.tempTextBoxY -= 100;   
                for(t = 1; t &lt;= global.locationEventNumber; t++)
                {
                    global.tempEventID[global.NumOfWeeksAtLocation] = string(t);
                    global.tempEvent2 = "Event"+global.tempEventID[global.NumOfWeeksAtLocation];
                    tempEventWeek = global.tempEvent2+"Week";
                    tempEventYear = global.tempEvent2+"Year";
                    tempEventType = global.tempEvent2+"Type";
                    ini_open(global.locationINI);//open the ini
                    global.eventWeek2 = ini_read_real(global.tempLocation, tempEventWeek, 0);//read the event week
                    global.eventYear2 = ini_read_real(global.tempLocation, tempEventYear, 1);//read the event week
                                        
                    ini_close();
                    if(global.eventWeek2 == global.locationWeekabc and global.eventYear2 == global.eventYear)
                    {
                        tempArray[global.NumOfWeeksAtLocation] = tempEventType; 
                        if(global.NumOfWeeksAtLocation &lt; 3)
                            global.NumOfWeeksAtLocation += 1;
                            
        
                    }
        
                }
                for(i = 8; i &lt; 8 + global.NumOfWeeksAtLocation; i++)
                {
                     ini_open(global.locationINI);//open the ini
        
                global.eventType2[i - 8] = ini_read_real(global.tempLocation, tempArray[i - 8], 0);//read the event type
                     ini_close();
                    global.textboxArray[i] = instance_create(global.tempInputBoxX + 400 ,global.tempInputBoxY,TextInputBox);  //create an instance at the location using the object 
                    global.tempInputBoxY += 25; //incrase the position of the text boxes
                    global.tempTextBoxY += 25;   
                }
            }
        }
    }
    else
    {
    global.tempInputBoxY += 85;
        global.tempTextBoxY += 85;
        //this displays the text on the location add screen (once you click on a location to edit its information)
        textFeilds = 4;//number of text boxes we want.
        ini_open(global.locationINI)//open ini file
        global.realLocationName = ini_read_string(global.tempLocation,"Real Location Name",""); //Reads the name of location from ini
        global.description = ini_read_string(global.tempLocation,"Description","");//reads the description from ini
        global.locationMyth = ini_read_real(global.tempLocation, "Mythical", "");//reads the mythical status from ini
        global.locationEventNumber = ini_read_real(global.tempLocation, "Number of Events",0); //reads the number of events from ini
        ini_close();//closes the ini
        for(i = 0; i &lt; textFeilds; i++)//for the number of text boxes set above
        {
            if(i == 1)//if i = description box then it will set the position based on the increased size
            { 
                global.textboxArray[i] = instance_create(global.tempInputBoxX,global.tempInputBoxY,TextInputBox);  //creats an instance at the location using the object 
                global.tempInputBoxY += 100;//increases the location of the text/input boxes (large increase for multiple line)
                global.tempTextBoxY += 100;
            }
            else
            {
                //creates the instanes of the text boxes on screen.
                global.textboxArray[i] = instance_create(global.tempInputBoxX ,global.tempInputBoxY,TextInputBox);  //creats an instance at the location using the object 
        
            }
                global.tempInputBoxY += 25; //increases the location of the text/input boxes (standard increase for one line)
                global.tempTextBoxY += 25;
        }
        if(global.locationEventNumber &gt; 0)//if there are events
        {
            global.tempInputBoxY += 75; //set the position 
            global.tempTextBoxY += 75;   
           
            global.tempEvent = "Event1";//setup the event name for accessig the ini
            tempEventType = global.tempEvent+"Type";
            tempEventWeek = global.tempEvent+"Week";
            tempEventYear = global.tempEvent+"Year";
            tempEventmyth = global.tempEvent+"Mythical";
            ini_open(global.locationINI);//open the ini
            global.eventType = ini_read_real(global.tempLocation, tempEventType, 0);//read the event type
            global.eventWeek = ini_read_real(global.tempLocation, tempEventWeek, 0);//read the event week
            global.eventYear = ini_read_real(global.tempLocation, tempEventYear, 1);//read the event week
            global.eventMyth = ini_read_real(global.tempLocation, tempEventmyth, 0);//read the event mythical status
            ini_close();
            global.oldWeek = global.eventWeek;
            global.locationWeekabc = global.eventWeek;
            for(i = 4; i &lt; 7; i++)//for the number of feilds (type,week,mythical)
            {
                global.textboxArray[i] = instance_create(global.tempInputBoxX ,global.tempInputBoxY,TextInputBox);  //create an instance at the location using the object 
                global.tempInputBoxY += 25; //incrase the position of the text boxes
                global.tempTextBoxY += 25;   
            }
            if(global.locationWeekabc != 0)
            {
                global.NumOfWeeksAtLocation = 1;
                global.tempInputBoxY -= 75; //incrase the position of the text boxes
                global.tempTextBoxY -= 75;   
   
                for(i = 7; i &lt; 7 + global.NumOfWeeksAtLocation; i++)
                {
                     ini_open(global.locationINI);//open the ini
        
                global.eventType2[i - 7] = ini_read_real(global.tempLocation, tempArray[i - 7], 0);//read the event type
                     ini_close();
                    global.textboxArray[i] = instance_create(global.tempInputBoxX + 400 ,global.tempInputBoxY,TextInputBox);  //create an instance at the location using the object 
                    global.tempInputBoxY += 25; //incrase the position of the text boxes
                    global.tempTextBoxY += 25;   
                }
            }
        }
    } 
       
}

if(room == RoomCardEdit)
{



    global.tempInputBoxY += 85;
    global.tempTextBoxY += 85;
    if(global.RoomState == 0)//event card
    {
        //works the same as above, but for roomEventAdd and roomEvenEdit, this is used to display the events information 
        if(global.TutorialSetup == false)
            global.tempEventID = string(global.eventRef);// takes the current cardRef (set when accessing the room)
        else
        {
            global.eventRef = "T";
            global.tempEventID = "T";
        }
        global.eventName = "Event"+global.tempEventID 
        ini_open(global.cardEventINI)
        global.realEventName = ini_read_string(global.eventName,"Event Name",""); //writes the instance number (global TotalInstanceNum) to "instanceRef" inside the ini file.;
        global.reqEventInt = ini_read_real(global.eventName,"Int",0);
        global.reqEventChar = ini_read_real(global.eventName, "Char", 0);
        global.reqEventWell = ini_read_real(global.eventName, "Well",0);
        global.eventMythical = ini_read_real(global.eventName,"Mythical",0);
        global.cardSprite = ini_read_real(global.eventName,"Sprite",-1);
        global.eventActionCardsNum = ini_read_real(global.eventName,"Action Cards",0);
        global.card1[global.eventActionCardsNum] = 0;
        for(i = 0; i &lt;= global.eventActionCardsNum; i++)
        {
            tempAction = "Action"+string(i+1);
            global.card1[i] = ini_read_real(global.eventName,tempAction,0);

        }
        //global.card2 = ini_read_real(global.eventName,"Cons2",0);
        global.InfoCard = ini_read_real(global.eventName,"Info Cards",0);
        global.CharCard = ini_read_real(global.eventName,"Char Cards",0);
        textFeilds = global.eventActionCardsNum + 8;//number of text boxes we want.
        ini_close();
        global.box0 = instance_count;
        global.box1 = instance_count+1;
        global.box2 = instance_count+2;
        global.box3 = instance_count+3;
        global.box4 = instance_count+4;
        global.box5 = instance_count+5;
        global.box6 = instance_count+6;
        global.box7 = instance_count+7;
        global.box8 = instance_count+8;
        global.box9 = instance_count+9;
        for(i = 0; i &lt; textFeilds; i++)
        {
                //creates the instanes of the text boxes on screen.
                global.textboxArray[i] = instance_create(global.tempInputBoxX ,global.tempInputBoxY,TextInputBox);  //creats an instance at the location using the object 


            global.tempInputBoxY += 25; //increases the location of the text/input boxes (standard increase for one line)
            global.tempTextBoxY += 25;
        }
    }
    if(global.RoomState == 1)//action card
    {
        if(global.TutorialSetup == false)
            global.tempCardID = string(global.cardRef);// takes the current cardRef (set when accessing the room)
        else
        {
            global.cardRef = "T";
            global.tempCardID = "T";
        }        
        //displays the cards information on the card add screen, same way as above.
        global.cardName = "Action"+global.tempCardID;
        textFeilds = 5;//number of text boxes we want.
        ini_open(global.cardActionINI)
        global.realCardName = ini_read_string(global.cardName,"Card Name","");
        global.cardText = ini_read_string(global.cardName,"Card Text","");
        global.cardMythical = ini_read_real(global.cardName,"Mythical",0);
        global.cardInfoSlot = ini_read_real(global.cardName, "Info Cards",0);
        global.cardSprite = ini_read_real(global.cardName,"Sprite",-1);
        global.ConsCard = ini_read_real(global.cardName,"Cons Cards",0);
        ini_close();
        var inst;
        for(i = 0; i &lt; textFeilds; i++)
        {
            global.textboxArray[i] = instance_create(global.tempInputBoxX,global.tempInputBoxY,TextInputBox);  //creats an instance at the location using the object 
            global.tempInputBoxY += 25;
            global.tempTextBoxY += 25;
            if(i == 2)
            {
                global.tempInputBoxY += 100;
                global.tempTextBoxY += 100;  
            }
        }
    }
    if(global.RoomState == 2)//cons card
    {
        if(global.TutorialSetup == false)
            global.tempConID = string(global.consCardRef);// takes the current cardRef (set when accessing the room)
        else
        {
            global.consCardRef = "T";
            global.tempConID = "T";
        }
        //displays the cards information on the card add screen, same way as above.
        global.consName = "Cons"+global.tempConID;
        textFeilds = 5;//number of text boxes we want.
        ini_open(global.cardConsINI)
        global.realConsName = ini_read_string(global.consName,"Card Name","");        
        global.consInt = ini_read_real(global.consName ,"Int",0);
        global.consChar = ini_read_real(global.consName ,"Char",0);
        global.consWell = ini_read_real(global.consName ,"Well",0);
        global.cardSprite = ini_read_real(global.consName,"Sprite",-1);
        global.cardInfoSlot = ini_read_real(global.consName, "Info Cards",0);
        ini_close();
        var inst;
        
        for(i = 0; i &lt; textFeilds; i++)
        {

                //creates the instanes of the text boxes on screen.
                global.textboxArray[i] = instance_create(global.tempInputBoxX ,global.tempInputBoxY,TextInputBox);  //creats an instance at the location using the object 
            global.tempInputBoxY += 25; //increases the location of the text/input boxes (standard increase for one line)
            global.tempTextBoxY += 25;
        }
    }
    if(global.RoomState == 3)//info card
    {
        if(global.TutorialSetup == false)
            global.tempInfoID = string(global.infoCardRef);// takes the current cardRef (set when accessing the room)
        else
        {
            global.infoCardRef = "T";
            global.tempInfoID = "T";
        }
        //displays the cards information on the card add screen, same way as above.
        global.infoName = "Info"+global.tempInfoID;
        textFeilds = 3;//number of text boxes we want.
        ini_open(global.cardInfoINI)
        global.realInfoName = ini_read_string(global.infoName,"Card Name","");
        global.infoCardText = ini_read_string(global.infoName,"Card Text","");
        global.cardSprite = ini_read_real(global.infoName,"Sprite",-1);
        global.infoLink = ini_read_string(global.infoName,"Link","");
        ini_close();
        var inst;
        
        for(i = 0; i &lt; textFeilds; i++)
        {
            if(i != 0)//if i = description box then it will set the position based on the increased size
            { 
                global.textboxArray[i] = instance_create(global.tempInputBoxX,global.tempInputBoxY,TextInputBox);  //creats an instance at the location using the object 
                global.tempInputBoxY += 100;//increases the location of the text/input boxes (large increase for multiple line)
                global.tempTextBoxY += 100;
            }
            else
            {
                //creates the instanes of the text boxes on screen.
                global.textboxArray[i] = instance_create(global.tempInputBoxX ,global.tempInputBoxY,TextInputBox);  //creats an instance at the location using the object 
            }
            global.tempInputBoxY += 25; //increases the location of the text/input boxes (standard increase for one line)
            global.tempTextBoxY += 25;
        }
    }
    if(global.RoomState == 4)//CHAR card
    {
        if(global.TutorialSetup == false)
            global.tempCharID = string(global.charCardRef);// takes the current cardRef (set when accessing the room)
        else
        {
            global.charCardRef = "T";
            global.tempCharID = "T";
        }
        //displays the cards information on the card add screen, same way as above.
        global.charName = "Char"+global.tempCharID;
        textFeilds = 3;//number of text boxes we want.
        ini_open(global.cardCharINI)
        global.charCardName = ini_read_string(global.charName,"Card Name","");
        global.charCardText = ini_read_string(global.charName,"Card Text","");
        global.cardSprite = ini_read_real(global.charName,"Sprite",-1);
        global.cardInfoSlot = ini_read_real(global.charName,"Info Cards",0);
        ini_close();
        var inst;
        
        for(i = 0; i &lt; textFeilds; i++)
        {
            if(i != 0)//if i = description box then it will set the position based on the increased size
            { 
                global.textboxArray[i] = instance_create(global.tempInputBoxX,global.tempInputBoxY,TextInputBox);  //creats an instance at the location using the object 
                global.tempInputBoxY += 100;//increases the location of the text/input boxes (large increase for multiple line)
                global.tempTextBoxY += 100;
            }
            else
            {
                //creates the instanes of the text boxes on screen.
                global.textboxArray[i] = instance_create(global.tempInputBoxX ,global.tempInputBoxY,TextInputBox);  //creats an instance at the location using the object 
            }
            global.tempInputBoxY += 25; //increases the location of the text/input boxes (standard increase for one line)
            global.tempTextBoxY += 25;
        }
    }
}
if(room == RoomCardDeck)
{
background_xscale[0] = 1 * ((room_width) / background_width[0]);
background_yscale[0] = 1 * ((room_height) / background_height[0]);
background_xscale[1] = 1 * ((room_width) / background_width[1]);
background_yscale[1] = 1 * ((room_height) / background_height[1]);
background_xscale[2] = 1 * ((room_width) / background_width[2]);
background_yscale[2] = 1 * ((room_height) / background_height[2]);
background_xscale[3] = 1 * ((room_width) / background_width[3]);
background_yscale[3] = 1 * ((room_height) / background_height[3]);
background_xscale[4] = 1 * ((room_width) / background_width[4]);
background_yscale[4] = 1 * ((room_height) / background_height[4]);
background_xscale[5] = 1 * ((room_width) / background_width[5]);
background_yscale[5] = 1 * ((room_height) / background_height[5]);

    global.tempInputBoxY += 25;
    global.tempTextBoxY += 25;
    if(global.RoomState == 0)//event card
    {
        if(global.ListStyle == 0)
        {
            //this code is to make sure that the list length doesnt exceed 20
            if(global.totalEventCards  &gt;= 20)
            {
                global.listLength = 20;   
            }
            else
            {
                global.listLength = 20 - (20 - global.totalEventCards);
            }
            //used to display the lists of events, works the same as previously
            for(i = global.tempPos; i &lt; global.listLength+global.tempPos; i++)
            {
                tempEventRef = string(i+1);                   
                tempEvent = "Event"+tempEventRef;
                ini_open(global.cardEventINI);
                global.eventRef = ini_read_real(tempEvent,"Ref","");
                global.eventText = ini_read_string(tempEvent,"Event Text","");
                global.realEventName = ini_read_string(tempEvent,"Event Name","");
                global.reqEventInt = ini_read_real(tempEvent,"Int",0);
                global.reqEventChar = ini_read_real(tempEvent, "Char", 0);       
                global.reqEventWell = ini_read_real(tempEvent, "Well",0);
                global.eventMythical = ini_read_real(tempEvent,"Mythical",0);
                global.cardSprite = ini_read_real(tempEvent,"Sprite",-1);
                global.eventActionCardsNum = ini_read_real(tempEvent,"Action Cards",0);
                ini_close();
                inst = instance_create(global.tempInputBoxX,global.tempInputBoxY,TextInputBox);     
                global.tempInputBoxY += 25;
                global.tempTextBoxY += 25;
            }
        }
        if(global.ListStyle == 1)
        {
            //this code is to make sure that the list length doesnt exceed 20
            if(global.totalEventCards  &gt;= 4)
            {
                global.listLength = 4;   
            }
            else
            {
                global.listLength = 4 - (4 - global.totalEventCards);
            }
            //used to display the lists of events, works the same as previously
            for(i = global.tempPos; i &lt; global.listLength+global.tempPos; i++)
            {
                tempEventRef = string(i+1);                   
                tempEvent = "Event"+tempEventRef;
                ini_open(global.cardEventINI);
                global.eventRef = ini_read_real(tempEvent,"Ref","");
       global.realEventName = ini_read_string(tempEvent,"Event Name",""); //writes the instance number (global TotalInstanceNum) to "instanceRef" inside the ini file.;
        global.eventText = ini_read_string(tempEvent,"Event Text","");
        global.reqEventInt = ini_read_real(tempEvent,"Int",0);
        global.reqEventChar = ini_read_real(tempEvent, "Char", 0);
            global.eventMythical = ini_read_real(tempEvent,"Mythical",0);
        global.reqEventWell = ini_read_real(tempEvent, "Well",0);
                global.cardSprite = ini_read_real(tempEvent,"Sprite",-1);
                ini_close();
                tempY = global.roomHeight/2;
                tempX = (global.roomWidth / 6)-25;
                inst = instance_create(tempX + 300*(i-global.tempPos),tempY,CardBacking);     
            }
        }
    }
    if(global.RoomState == 1)//Action card
    {
        if(global.ListStyle == 0)
        {
            //this code is to make sure that the list length doesnt exceed 20
            if(global.totalActionCards  &gt;= 20)
            {
                global.listLength = 20;   
            }
            else
            {
                global.listLength = 20 - (20 - global.totalActionCards);
            }
            if(global.lastState != 0)
            {
                eventMythical = 2;  
            }
            else
            {
                ini_open(global.cardEventINI);
                eventMythical = ini_read_real(global.eventName,"Mythical", 0);
                ini_close();  
            }
             //used to display the lists of card, works the same as previously
            for(i = global.tempPos; i &lt; global.listLength+global.tempPos; i++)
            {
                tempCardRef = string(i+1);       
                tempCard = "Action"+tempCardRef;
                ini_open(global.cardActionINI);
                global.cardRef = ini_read_real(tempCard,"Ref",0);
                global.realCardName = ini_read_string(tempCard,"Card Name","");
                global.cardMythical = ini_read_real(tempCard,"Mythical",0);
                 global.cardSprite = ini_read_real(tempCard,"Sprite",-1);
                global.cardInfoSlot = ini_read_real(tempCard, "Info Cards",0);
                ini_close();
                if(global.cardMythical == eventMythical or eventMythical == 2)
                {
               global.textboxArray[i] = instance_create(global.tempInputBoxX,global.tempInputBoxY,TextInputBox);     
                    global.tempInputBoxY += 25;
                    global.tempTextBoxY += 25;
                }
            }
        }
        if(global.ListStyle == 1)
        {
                    //this code is to make sure that the list length doesnt exceed 20
            if(global.totalActionCards  &gt;= 4)
            {
                global.listLength = 4;   
            }
            else
            {
                global.listLength = 4 - (4 - global.totalActionCards);
            }
            if(global.lastState != 0)
            {
                eventMythical = 2;  
            }
            else
            {
                ini_open(global.cardEventINI);
                eventMythical = ini_read_real(global.eventName,"Mythical", 0);
                ini_close();  
            }
            tempCount = 0;
             //used to display the lists of card, works the same as previously
            for(i = global.tempPos; i &lt; global.listLength+global.tempPos; i++)
            {
                tempCardRef = string(i+1);       
                tempCard = "Action"+tempCardRef;
                ini_open(global.cardActionINI);
                global.cardRef = ini_read_real(tempCard,"Ref",0);
                global.realCardName = ini_read_string(tempCard,"Card Name","");
                global.cardText = ini_read_string(tempCard,"Card Text","");
                global.cardSprite = ini_read_real(tempCard,"Sprite",-1);
                global.cardMythical = ini_read_real(tempCard,"Mythical",0);
                ini_close();
                if(global.cardMythical == eventMythical or eventMythical == 2)
                {

                    tempY = global.roomHeight/2;
                    tempX = (global.roomWidth / 6)-25;
                    inst = instance_create(tempX + 300*(tempCount),tempY,CardBacking);
                    tempCount += 1;     
                    
                }
            }
        }
    }
    if(global.RoomState == 2)//Consequence
    {
        if(global.ListStyle == 0)
        {
            //this code is to make sure that the list length doesnt exceed 20
            if(global.totalConsCards  &gt;= 20)
            {
                global.listLength = 20;   
            }
            else
            {
                global.listLength = 20 - (20 - global.totalConsCards);
            }
             //used to display the lists of card, works the same as previously
            for(i = global.tempPos; i &lt; global.listLength+global.tempPos; i++)
            {
                tempConsCardRef = string(i+1);       
                tempConsCard = "Cons"+tempConsCardRef;
                ini_open(global.cardConsINI);
                global.consCardRef = ini_read_real(tempConsCard,"Ref",0);
                global.realConsName = ini_read_string(tempConsCard,"Card Name","");   
                global.consCardText = ini_read_string(tempConsCard,"Card Text","");
                global.consInt = ini_read_real(tempConsCard,"Int",0);
                global.consChar = ini_read_real(tempConsCard,"Char",0);
                global.consWell = ini_read_real(tempConsCard,"Well",0);
                global.cardSprite = ini_read_real(tempConsCard,"Sprite",-1);
                global.cardInfoSlot = ini_read_real(tempConsCard, "Info Cards",0);
                ini_close();
                global.textboxArray[i] = instance_create(global.tempInputBoxX,global.tempInputBoxY,TextInputBox);     
                global.tempInputBoxY += 25;
                global.tempTextBoxY += 25;
            
            }
        }
        if(global.ListStyle == 1)
        {
        //this code is to make sure that the list length doesnt exceed 20
            if(global.totalConsCards  &gt;= 4)
            {
                global.listLength = 4;   
            }
            else
            {
                global.listLength = 4 - (4 - global.totalConsCards);
            }
             //used to display the lists of card, works the same as previously
            for(i = global.tempPos; i &lt; global.listLength+global.tempPos; i++)
            {
                tempConsCardRef = string(i+1);       
                tempConsCard = "Cons"+tempConsCardRef;
                ini_open(global.cardConsINI);
                global.consCardRef = ini_read_real(tempConsCard,"Ref",0);
                global.realConsName = ini_read_string(tempConsCard,"Card Name","");   
                global.consInt = ini_read_real(tempConsCard,"Int",0);
                global.consChar = ini_read_real(tempConsCard,"Char",0);
                global.consWell = ini_read_real(tempConsCard,"Well",0);
                global.cardSprite = ini_read_real(tempConsCard,"Sprite",-1);
                ini_close();
                tempY = global.roomHeight/2;
                tempX = (global.roomWidth / 6)-25;
                inst = instance_create(tempX + 300*(i-global.tempPos),tempY,CardBacking);     
                
            
            }
        }
    }
    if(global.RoomState == 3)//Info
    {
        if(global.ListStyle == 0)
        {
            //this code is to make sure that the list length doesnt exceed 20
            if(global.totalInfoCards  &gt;= 20)
            {
                global.listLength = 20;   
            }
            else
            {
                global.listLength = 20 - (20 - global.totalInfoCards);
            }
             //used to display the lists of card, works the same as previously
            for(i = global.tempPos; i &lt; global.listLength+global.tempPos; i++)
            {
                tempInfoCardRef = string(i+1);       
                tempInfoCard = "Info"+tempInfoCardRef;
                ini_open(global.cardInfoINI);
                global.infoCardRef = ini_read_real(tempInfoCard,"Ref",0);
                global.realInfoName = ini_read_string(tempInfoCard,"Card Name","");
                global.infoCardText = ini_read_string(tempInfoCard,"Card Text","");
                global.cardSprite = ini_read_real(tempInfoCard,"Sprite",-1);
                global.infoLink = ini_read_string(tempInfoCard,"Link","");
                
                ini_close();
                global.textboxArray[i] = instance_create(global.tempInputBoxX,global.tempInputBoxY,TextInputBox);     
                global.tempInputBoxY += 25;
                global.tempTextBoxY += 25;
            
            }
        }
        if(global.ListStyle == 1)
        {
            //this code is to make sure that the list length doesnt exceed 20
            if(global.totalInfoCards  &gt;= 4)
            {
                global.listLength = 4;   
            }
            else
            {
                global.listLength = 4 - (4 - global.totalInfoCards);
            }
             //used to display the lists of card, works the same as previously
            for(i = global.tempPos; i &lt; global.listLength+global.tempPos; i++)
            {
                tempInfoCardRef = string(i+1);       
                tempInfoCard = "Info"+tempInfoCardRef;
                ini_open(global.cardInfoINI);
                global.infoCardRef = ini_read_real(tempInfoCard,"Ref",0);
                global.realInfoName = ini_read_string(tempInfoCard,"Card Name","");
                global.infoCardText = ini_read_string(tempInfoCard,"Card Text","");
                global.cardSprite = ini_read_real(tempInfoCard,"Sprite",-1);
                global.infoLink = ini_read_string(tempInfoCard,"Link",0);
                
                ini_close();
                tempY = global.roomHeight/2;
                tempX = (global.roomWidth / 6)-25;
                inst = instance_create(tempX + 300*(i-global.tempPos),tempY,CardBacking);     
              }  
        }
    }
    if(global.RoomState == 4)//Character
    {
        if(global.ListStyle == 0)
        {
            //this code is to make sure that the list length doesnt exceed 20
            if(global.totalCharCards  &gt;= 20)
            {
                global.listLength = 20;   
            }
            else
            {
                global.listLength = 20 - (20 - global.totalCharCards);
            }
            //used to display the lists of card, works the same as previously
            for(i = global.tempPos; i &lt; global.listLength+global.tempPos; i++)
            {
                tempCharCardRef = string(i+1);       
                tempCharCard = "Char"+tempCharCardRef;
                ini_open(global.cardCharINI);
                global.charCardRef = ini_read_real(tempCharCard,"Ref",0);
                global.charCardName = ini_read_string(tempCharCard,"Card Name","");
                global.charCardText = ini_read_string(tempCharCard,"Card Text","");
                global.cardSprite = ini_read_real(tempCharCard,"Sprite",-1);
                global.cardInfoSlot = ini_read_real(tempCharCard, "Info Cards",0);
                ini_close();
                global.textboxArray[i] = instance_create(global.tempInputBoxX,global.tempInputBoxY,TextInputBox);     
                global.tempInputBoxY += 25;
                global.tempTextBoxY += 25;
            
            }
        }
        if(global.ListStyle == 1)
        {
        //this code is to make sure that the list length doesnt exceed 20
            if(global.totalCharCards  &gt;= 4)
            {
                global.listLength = 4;   
            }
            else
            {
                global.listLength = 4 - (4 - global.totalCharCards);
            }
            //used to display the lists of card, works the same as previously
            for(i = global.tempPos; i &lt; global.listLength+global.tempPos; i++)
            {
                tempCharCardRef = string(i+1);       
                tempCharCard = "Char"+tempCharCardRef;
                ini_open(global.cardCharINI);
                global.charCardRef = ini_read_real(tempCharCard,"Ref",0);
                global.charCardName = ini_read_string(tempCharCard,"Card Name","");
                global.charCardText = ini_read_string(tempCharCard,"Card Text","");
                global.cardSprite = ini_read_real(tempCharCard,"Sprite",-1);
                ini_close();
                tempY = global.roomHeight/2;
                tempX = (global.roomWidth / 6)-25;
                inst = instance_create(tempX + 300*(i-global.tempPos),tempY,CardBacking);     
                
            
            }
        }
    }
}


</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="6" enumb="53">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>//if anywhere is pressed on screen, it will change focus to 0 (so if outside of a button)
global.focus = 0;
if(room == RoomLocationAdd)
    script_execute(SaveLocation);
if(room == RoomCardEdit)
{
    if(global.RoomState == 0)
        script_execute(SaveEventCard);
    if(global.RoomState == 1)
        script_execute(SaveActionCard);
    if(global.RoomState == 2)
        script_execute(SaveConsCard);
    if(global.RoomState == 3)
        script_execute(SaveInfoCard);
    if(global.RoomState == 4)
        script_execute(SaveCharCard);
}
if(room == RoomTutorial)
{
    script_execute(SaveTutorial);
}
if(room != RoomMap)
{   
    room = tempRoom;
}
if(room == RoomCustomStats)
{
    ini_open(global.editorINI)
    ini_write_string("General", "Custom Int", global.customInt);
    ini_write_string("General", "Custom Well",  global.customWell);
    ini_write_string("General", "Custom Char", global.customChar);
    ini_write_real("General", "Custom Weeks", global.customMaxWeeks);
    ini_close();
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </events>
  <PhysicsObject>0</PhysicsObject>
  <PhysicsObjectSensor>0</PhysicsObjectSensor>
  <PhysicsObjectShape>0</PhysicsObjectShape>
  <PhysicsObjectDensity>0.5</PhysicsObjectDensity>
  <PhysicsObjectRestitution>0.100000001490116</PhysicsObjectRestitution>
  <PhysicsObjectGroup>0</PhysicsObjectGroup>
  <PhysicsObjectLinearDamping>0.100000001490116</PhysicsObjectLinearDamping>
  <PhysicsObjectAngularDamping>0.100000001490116</PhysicsObjectAngularDamping>
  <PhysicsObjectFriction>0.200000002980232</PhysicsObjectFriction>
  <PhysicsObjectAwake>-1</PhysicsObjectAwake>
  <PhysicsObjectKinematic>0</PhysicsObjectKinematic>
  <PhysicsShapePoints/>
</object>
